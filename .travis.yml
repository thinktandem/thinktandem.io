language: node_js
node_js:
  - '10'
services:
  - docker

before_install:
  - sudo apt-get -y update
  - sudo apt-get -y install cgroup-bin curl
  - curl -fsSL -o /tmp/lando.deb https://github.com/lando/lando/releases/download/v3.0.0-rc.18/lando-v3.0.0-rc.18.deb
  - sudo dpkg -i /tmp/lando.deb
  - lando version

script:
  - lando npm install
  - lando bower install -y
  - lando grunt build
  - lando start -- -v
  # - lando grunt test
